---
apiVersion: v1
kind: Service
metadata:
  name: proxy
  #namespace:  'proxy'
  labels:
    {{- include "proxy.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: proxy
  ports:
    - name: http
      port: 8080
      targetPort: 8080